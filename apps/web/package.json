{
  "name": "@arbitron/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vite static multi-page front end (vanilla JS ES modules, no framework)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "build:e2e": "vite build --mode e2e"
  },
  "devDependencies": {
    "vite": "^7.1.12"
  },
  "dependencies": {
    "@arbitron/core": "workspace:*",
    "@fontsource/inter": "^5.3.0",
    "@fontsource/jetbrains-mono": "^5.3.0"
  }
}
