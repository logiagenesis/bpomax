{
  "name": "@arbitron/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Supabase migrations, seed data and generated types",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing.ts",
    "./fixtures": "./src/fixtures.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "db:reset": "supabase db reset",
    "db:types": "supabase gen types typescript --local > src/types.generated.ts",
    "db:seed": "node -e \"console.error('Wiring the seed into supabase db reset lands with C-01 (docs/BLOCKERS.md). Until then the seed is applied through buildSeedSql() in packages/db/src/seed.ts, which is covered by seed.test.ts.'); process.exit(1)\""
  },
  "devDependencies": {
    "@types/node": "^26.6.2"
  },
  "dependencies": {
    "@arbitron/core": "workspace:*"
  }
}
