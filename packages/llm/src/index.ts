/**
 * @arbitron/llm — provider-agnostic LLM client.
 *
 * Model names and prices are read from config, never hard-coded (01 section C).
 * Built at ARB-031.
 */
export const PACKAGE_NAME = '@arbitron/llm';
