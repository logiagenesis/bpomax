# BLOCKERS — raised during the build

Document: LI-BLKBUILD-ARB-0926 — opened 22/09/2026
Companion to docs/02-BLOCKERS.md, which is the owner's pre-build action list. This file
holds blockers found while building, per docs/01 rule 6.

Status values: OPEN / CLEARED.
Types: CREDENTIAL (owner must supply a key or account), DECISION (owner must choose),
VERIFICATION (built and self-checked, but a claim in the acceptance criteria cannot be
proven in the build container), CONTRADICTION (the docs disagree with themselves).

| # | Type | Ticket | What is blocked | What is needed, and from whom | Status |
|---|---|---|---|---|---|
| C-01 | CREDENTIAL | ARB-010, ARB-012 | Only the hosted half of ARB-010 and ARB-012 is still blocked: types generated from the hosted project (`pnpm db:types:supabase`, which runs `supabase gen types --linked`), and applying the migrations to a real Supabase project. Everything local is proven. The migrations apply to real Postgres (PGlite) on every push. In CI's compose job, `pnpm db:reset` recreates the `supabase/postgres` image with an empty volume, applies all 16 migrations and seeds, and a second seed changes nothing (run 62). The committed types are generated from the migrated schema and drift-tested (D-038). | Owner: clear B-06 in docs/02-BLOCKERS.md — create the Supabase project and supply SUPABASE_URL, the anon key, the service role key and DATABASE_URL. Also needs D-05 (data region), which depends on the POPIA advice in T-06. | OPEN |
| V-02 | VERIFICATION | ARB-010, ARB-020 | Token encryption at rest. Cleared 23/09/2026: tokens are Supabase Vault secrets (migration 0017, D-041), and CI's compose job runs `scripts/db-verify-vault.sql` against the real `supabase/postgres` image: the stored secret is unreadable in `vault.secrets`, decrypts to the value written, is refused to `authenticated`, and is gone after a disconnect (run 72, and every run since). The RLS suite still runs against the shim of Supabase's `auth` schema (D-012). | Owner: nothing. Once B-06 is cleared, the same script can be run against the hosted project as a spot check. | CLEARED |
| D-14 | DECISION | ARB-013, ARB-040 | Seed figures for `market_price_bands`. docs/01 says to seed bands flagged `source='seed'` but gives no numbers, and ARB-040 prices real bids off the p50 — so an invented band becomes an invented price on a real proposal. The seed mechanism is built, idempotent and tested; it ships zero bands. The estimate worker falls through to the next method while there are none. | Owner: supply a p25/p50/p75 per category and currency you stand behind — from your own completed projects, a rate you would quote, or a CSV of observed marketplace prices — or confirm that bands stay empty until the price-refresh worker has real data. Numbering continues docs/02-BLOCKERS.md, whose decisions end at D-13. | OPEN |
| V-03 | VERIFICATION | ARB-012 | The hosted sign-in round trip itself: `signInWithPassword`, the emailed confirmation, and the JWT Supabase mints. Everything the schema does with that identity is proven here — the trigger that provisions an application user, the role split, and the rule that an approval names the person who made it — but no real token has ever been issued. | Owner: once B-06 is cleared, create one test user in the Supabase project and sign in from the login page (ARB-061); the Playwright e2e run then covers it. | OPEN |
| V-04 | VERIFICATION | ARB-031, ARB-032 | The Anthropic transport (`packages/llm/src/anthropic.ts`), and so the quality of a real model's scores: the score worker is proven end to end with a scripted model, which shows the plumbing and the red-flag rules work but says nothing about whether the model's own 0–100 judgements are sensible. No request has ever reached the API, because there is no key (B-08). Everything around it is proven against a scripted transport: schema validation, the single retry with errors fed back, metering of both attempts, and the price table. The transport is kept to ~40 lines so that what is unverified is small and obvious. | Owner: clear B-08, set ANTHROPIC_API_KEY in .env, then run one scoring call (ARB-032) and confirm the usage figures it records against the Console. | OPEN |
| C-02 | CREDENTIAL | ARB-020, ARB-022, ARB-120, ARB-203 | Narrowed 23/09/2026. Both tickets are built and tested end to end against an in-process stand-in of Freelancer.com that answers in the documented shapes, with every endpoint cited in code from developers.freelancer.com. ARB-020 (D-041, D-044): the OAuth connect, the callback, Vault token storage, refresh, disconnect and the one-account-per-identity rule. ARB-022 (D-045): one schedule per scanner, the documented project search with the scanner's filters, per-org upserts with no duplicates, `job.ingested` events and score jobs for new listings, rate limits logged and retried by the queue's backoff. ARB-120 (D-046): the inbox sync, one schedule per connected account, the documented thread and message lists, per-thread and per-message keys, `message.received` events and the Telegram card. Still open: each ticket's own clause, "works in sandbox", "new matching sandbox jobs appear within one interval" and "new sandbox message appears in app within one interval", which need a real developer app and sandbox accounts; the shape of the search's `user_details` projection (the listing's client fields stay null until a sandbox answer shows it), and the threads list's envelope (`result.threads`, `result.users`), which the docs page does not show. ARB-203 (D-055) added the employer's side: the documented currency lookup, skill search and project create, and the list of a project's bids, with a sourcing worker behind the live gate that posts an approved Freelancer.com draft and collects its bids into candidates with country and price. Its own clause, "Sandbox employer project created; candidate bids stored with country and price", needs the sandbox employer account; the bids list's envelope (`result.bids`, `result.users`) and where a bidder's country sits (`location.country.code`) are read defensively because the docs page prints no list response. | Owner: clear B-03 (developer app: client ID, secret, redirect URI `https://bpomax.vercel.app/freelancer-callback.html` or the local one, granted scopes 1, 2, 5, 6, and `fln:project_create` for ARB-203's employer projects) and B-04 (one sandbox freelancer and one sandbox employer account), and put the values in `.env` only. Then press Connect Freelancer.com on the settings page and add a scanner. | OPEN |
| D-15 | DECISION | ARB-061, and the sandbox clauses of ARB-044 and ARB-050 | The rest of Phase 1. Each ticket's acceptance rests on a figure or a decision that docs/01 rule 6 forbids inventing: ARB-040 is built (D-028, `6c1f43a`): its mechanism invents nothing, so it closed with D-04 and D-14 still open — until the owner ticks in-house categories, adds rate cards or supplies bands it records `skipped` with the reason, never a figure. ARB-041 is built (D-029): the engine reads T-02 (the fee table, each rule with its source page and date), D-02 (minimum margin) and D-03 (FX buffer) from `settings`, and a rate from the FX provider (B-10) for any deal not in ZAR; while any is missing it records `blocked` naming the rule, never a default, so until the owner answers those four items no job is judged; ARB-042 is built (D-030): the allowance is a column on the platform account, null until T-03 is answered, and a null allowance refuses every submission with a message naming T-03; ARB-043 is built (D-031): templates and portfolio items are owner rows, empty until D-07 and D-10; without an active template the worker records `blocked` naming D-07, and a citation can only be a recorded item, by schema and by foreign key; ARB-044 is built (D-032): approval, the scanner cap, the live gate on both switches, the allowance and the pipeline item are all in and tested with LIVE_MODE=false, and the platform call is an adapter — its sandbox clause waits on C-02 (the Freelancer client and accounts), and going live waits on T-01 (API terms) and B-05 (the verified account); ARB-050 is built (D-033): linking, the commands, the cards and Approve/Edit/Reject are tested against a scripted Telegram; the token (B-09) is read from `.env` at start, and "submits in sandbox" is the submit worker's call, on C-02. ARB-204 is built (D-056): a candidate's quote is judged by the same engine, and a bid on our own Freelancer.com project also needs T-02's employer-side rule, named when it is missing; ARB-061 is built (D-035): the six pages, their API routes and the Supabase token verifier are tested end to end against scripted services; signing in for real needs the Supabase project (B-06, V-03), and the pages show data only once the workers have rows to show. Every later phase depends on ARB-099, which needs all of Phase 1. | Owner: answer the items named, in docs/02-BLOCKERS.md. The highest-value first answers are T-02, D-02 and D-03, which unblock the margin engine, the core of the product. | OPEN |
| C-03 | CREDENTIAL | ARB-070, ARB-099 | Narrowed 23/09/2026. The web app is deployed: Vercel project `bpomax`, production from `main`, https://bpomax.vercel.app (B-11 answered). It runs in demo mode until the Supabase and API values exist (D-043). Still blocked: deploying the API, workers and Telegram bot, and so the API health URL, the reachable Telegram webhook, and the Phase 1 tag that needs them. | Owner: clear B-12 (a host for long-running Node processes: the API, the workers and the bot). Then set SUPABASE_URL, SUPABASE_ANON_KEY and API_URL in the Vercel project's environment, to switch the web app from demo mode to the real app. | OPEN |
| V-01 | VERIFICATION | ARB-004 | The acceptance clause "`docker compose up` healthy". The compose file is written and passes `docker compose config`, but this container has the Docker CLI without a running daemon (`/var/run/docker.sock` absent), so no image can be pulled or started. | Owner: run `docker compose up -d` on a machine with Docker running, confirm both services report healthy (`docker compose ps`), then set this row to CLEARED. Nothing in the repo needs changing first. Cleared 23/09/2026: CI's compose job runs `docker compose up -d --wait` on every push, and run 55 (`ececea4`) had both services healthy (D-037). | CLEARED |
| C-04 | CREDENTIAL | ARB-300 | Opened 24/09/2026. ARB-300 is built and tested end to end against a stand-in of Upwork that answers in the documented shapes, with every call cited in `packages/upwork` from the official GraphQL documentation (https://www.upwork.com/developer/documentation/graphql/api/docs/index.html): the OAuth authorisation code grant (connect, callback, Vault tokens, a refresh an hour before the 24-hour token lapses), the `marketplaceJobPostingsSearch` query with the scanner's filters, per-org upserts with `platform = upwork`, events and score jobs, a missing permission or refused token marking the account expired, and the CI check that no browser automation exists. Upwork jobs are read only: no bid is drafted, queued or sent for one (D-066). Still open: a real key, and the terms. Upwork reviews every key; its stated conditions include completed identity verification, at least USD 25,000 in lifetime earnings or spend, a Job Success Score of at least 90 % for freelancers and agencies, an account in good standing, staying within 40,000 requests a day, and not using Upwork's name or marks ("#getting-started-preparation"). Its terms allow no caching or storing of its data for more than 24 hours, so every Upwork job not fetched again within 24 hours is deleted (D-066); whether scores and estimates derived from a listing may be kept longer is the owner's reading of T-04. | Owner: B-14, apply for an Upwork API key with the permissions "Common Entities - Read-Only Access" and "Read marketplace Job Postings", callback URL `https://bpomax.vercel.app/upwork-callback.html` (or the local one), and put UPWORK_CLIENT_ID and UPWORK_CLIENT_SECRET in `.env` only; T-04, read https://www.upwork.com/legal#api and record whether the 24-hour deletion is enough. Then press Connect Upwork on the settings page and add an Upwork scanner. | OPEN |

## Notes

- V-01 does not block later tickets. Application code is written against `DATABASE_URL`
  and `REDIS_URL`, so it does not care whether those point at compose, the Supabase CLI,
  or hosted instances.
